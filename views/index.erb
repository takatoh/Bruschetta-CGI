<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title><%= @title %></title>
  </head>
  <body>
    <header>
      <h1><a href="index.cgi"><%= @title %></a></h1>
    </header>
    <main>
      <div id="search-box">
        <input type="text" id="search-string"><input type="submit" value="Search" id="search-button">
      </div>
      <div>
        <table>
          <% @books.each do |book| %>
          <tr><td><div class="book-title"><a href="index.cgi?id=<%= book["id"] %>"><%= title_with_vol(book) %></a></div>
          <div class="book-author"><%= book["author"] %></div></td></tr>
          <% end %>
        </table>
      </div>
    </main>

    <script>
      document.addEventListener("DOMContentLoaded", () => {
        const searchButton = document.getElementById("search-button");
        searchButton.addEventListener("click", () => {
          const searchString = document.getElementById("search-string").value;
          window.location.href = "index.cgi?search=" + searchString;
        });
      });
    </script>
  </body>
</html>
