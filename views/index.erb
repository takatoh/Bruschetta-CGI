<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @title %></title>
  </head>
  <body>
    <h1><%= @title %></h1>
    <div>
      <ul>
        <% @books.each do |book| %>
        <li><%= title_with_vol(book) %> (<%= book["author"] %>)</li>
        <% end %>
      </ul>
    </div>
  </body>
</html>
